<!doctype html>
<html lang="ja">
<head>
  <title>{{title}}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>
<body class="container mt-2">
  <h1 class="display-6 text-primary">
    <strong>{{title}}</strong>
  </h1>
  <hr class="my-2">

  @!section('body')

  <div class="my-3">&nbsp</div>
  <div style="width: 100%"
       class="fixed-bottom bg-secondarundefinedy p-1">
    <p class="text-center text-white m-0">
      copyright 2022 Kondo-Takahiro.
    </p>
  </div>
<header class="row">
  <div class="col-3 d-flex align-item-end">
    @if (session.get('code'))
    <a href="{{ route('auth.logout') }}" class="badge bg-secondary">ログアウト</a>
    @else
    <a href="{{ route('auth.login.show') }}" class="badge bg-primary">ログイン</a>
    @end
  </div>
</header>
</body>
</html>
